#include <stdio.h>
#include <conio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
	char str[128];
	printf("String: ");
	gets_s(str, sizeof(str));
	for( int i = 0; i < '\0'; i++)
	
	_getch();
	return 0;
}
